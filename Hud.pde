public class Hud  {

    private PVector gridCornerBegin;

    public Hud () {
        
    }
    //Affiche le score
    //Affiche le niveau
    //Affiche le next Shape

    public void plot() {
        plotScore(0);
        plotLevel(0);
        plotNextShape(null);
    }

    private void plotScore(int score){
        // to do
    }
    private void plotLevel(int level) {
        // to do 
    }
    private void plotNextShape(Shape nextShape) {

    }
}
